language: python
sudo: required

services:
  - docker

before_install:
  - pip install ansible==2.6.1

script:
  - ansible-playbook -i tests/inventory tests/test.yml
  - ansible-playbook -i tests/inventory tests/test_prepare.yml
  - ansible-playbook -i tests/cluster_inventory tests/test_role.yml
